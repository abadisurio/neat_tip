import 'package:flutter/material.dart';

class VehicleEnroll extends StatefulWidget {
  const VehicleEnroll({Key? key}) : super(key: key);

  @override
  State<VehicleEnroll> createState() => _VehicleEnrollState();
}

class _VehicleEnrollState extends State<VehicleEnroll> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
