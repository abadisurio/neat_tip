import 'package:flutter/material.dart';

class Suspend extends StatelessWidget {
  const Suspend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('suspend screen'),
    );
  }
}
